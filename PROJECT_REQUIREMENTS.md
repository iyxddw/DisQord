# DisQord 项目需求与实施规格

> **当前个人部署覆盖规则（2026-07-29）：**用户已明确取消 Docker、PostgreSQL 和应用层密钥
> 加密。三套程序以原生 Node.js/systemd 运行；中央端全部持久状态统一明文写入
> `data/central.json`（生产路径 `/var/lib/disqord/central/central.json`），QQ/Discord 节点继续
> 使用各自 `data` 目录。API 不得向浏览器回显完整 API Key，日志仍不得记录密钥。本文后续仍
> 出现的 PostgreSQL、Docker、加密字段或数据库迁移要求均由本覆盖规则取代。

> 状态：已批准并进入实现  
> 文档目的：记录需求、技术边界、实施顺序与验收标准。  
> 项目目标：以固定的中央服务端、QQ 节点和 Discord 节点三套程序，实现指定 QQ 群与 Discord 频道之间的双向消息互通。

---

## 1. 项目结论

项目在技术上可实现，采用固定的“三程序 + 中央编排”架构，不允许 QQ 节点和 Discord 节点切换为服务端：

- **中央服务端：**唯一公网入口和权威控制面板，负责节点注册、会话验证、蓝图路由、持久队列、大模型翻译与审核、图片卡片渲染、日志和审计。
- **QQ 节点：**只负责 QQ 登录、QQ 消息收发、媒体暂存、消息标准化、本地可靠队列及基础诊断。
- **Discord 节点：**只负责 Discord Bot 登录、Discord 消息收发、媒体暂存、消息标准化、本地可靠队列及基础诊断。
- QQ、Discord 节点都主动连接中央服务端，不要求公网 IP 或端口映射。
- 三套程序职责固定；节点之间不得直接通信。
- 节点通过加密 WebSocket（WSS）连接中央服务端，并支持断线重连、心跳、确认、重试和去重。
- 中央服务端提供主要 Web 控制面板；两个节点各自提供仅用于本端连接、凭据、运行状态和故障诊断的轻量面板。
- 转发到目标平台的用户消息统一渲染为图片卡片，卡片包含来源平台、发送者头像、昵称、时间、原文、译文及消息内容。

推荐中央服务端单独部署在有公网 IP 的第三台服务器上。为节省初期成本，也允许把中央服务端与某一节点暂时部署在同一台机器，但它们仍是独立程序、独立 systemd 服务、独立数据目录和固定角色，以便以后无缝迁移。

需要特别说明：Discord Bot 可使用官方接口；QQ 端如果接入普通个人 QQ，通常会依赖非官方协议实现，存在账号风控、掉线和协议失效风险。服务器隔离能够降低故障相互影响，但不能消除 QQ 账号本身的风控风险。

---

## 2. 范围

### 2.1 本期必须实现

1. QQ 群消息与 Discord 频道消息双向转发。
2. 中央服务端、QQ 节点和 Discord 节点为三套职责固定的程序。
3. QQ 与 Discord 节点分别部署在不同服务器；中央服务端推荐部署在第三台公网服务器。
4. 两个节点只主动连接中央服务端，节点之间不直接连接。
5. 中央服务端提供完整 Web 控制面板，两个节点各自提供轻量 Web 控制面板。
6. 支持文字、图片、文字加图片和回复消息。
7. 其他消息类型转发为写有“不支持的消息”的图片卡片。
8. 翻译和内容审核由中央服务端调用大模型完成。
9. 基础设置支持配置大模型 API，高级模式支持编辑翻译和审核提示词。
10. QQ 与 Discord 两侧的所有转发结果均只发送渲染后的图片卡片，不附带额外的原始文本消息。
11. 提供蓝图式图形编辑器，通过拖放节点和连线配置消息转发规则。
12. 提供独立的“聊天会话”页面，保存已验证的 QQ 群和 Discord 频道。
13. 添加聊天会话时向目标群或频道发送一次性验证码，管理员回填正确验证码后完成验证。
14. 提供自动审核、人工审核队列、完整日志、检索、导出、失败重试与基本运行指标。
15. 支持一对一、一对多和多对一转发，并允许对每条蓝图独立启停和限速。
16. 防止机器人转发自己产生的消息，避免消息循环。

### 2.2 默认不在首期实现

- 语音、视频、文件、红包、转账、戳一戳、表情包语义还原、投票、帖子、线程等消息的原生互转。
- QQ 私聊与 Discord 私信互通。
- 历史消息全量同步。
- 群成员与 Discord 成员身份绑定。
- 多主高可用、自动故障转移和跨区域集群。
- 通过本项目远程控制整台服务器。
- 节点之间直接连接或互换中央服务端角色。
- 将 QQ/Discord 节点的轻量面板直接无鉴权暴露到公网。
- 蓝图中的任意脚本执行、自定义代码节点和带环路的工作流。

以上功能以后可扩展，但不得影响首期数据结构的兼容性。

---

## 3. 需要审批的决策项

如果用户没有另行修改，后续实现采用每项的“默认建议”。

### D1. QQ 接入方式

- **已确认：**QQ 端固定使用 NapCat，并通过 OneBot 11 WebSocket 与 QQ 节点通信。
- NapCat 与本项目 QQ 节点保持独立进程或独立容器；NapCat 的管理端口与 OneBot 端口只允许本机或受信容器网络访问。
- 首期只实现 NapCat 适配与兼容性测试，不同时维护其他 QQ 协议实现分支。
- 本项目内部仍以标准化消息模型隔离 NapCat 事件，避免协议细节扩散到中央服务端。

风险接受条件：

- 使用专用小号，不使用重要私人账号。
- 禁止高频刷屏，设置全局和单路由限速。
- NapCat 与 QQ 节点分进程或分容器运行。
- 登录凭据、二维码、令牌不得写入日志或提交 Git。

### D2. 大模型翻译

- **默认建议：**中央服务端使用可配置的大模型 API 自动识别语言；中文翻译为英文，英文翻译为中文，其他语言默认翻译为中文。
- 卡片同时保留原文和译文，避免机器翻译造成语义丢失。
- 图片内文字首期不做 OCR 和翻译。
- 纯图片消息只展示图片和元信息，不凭空生成译文。
- 大模型必须返回结构化结果；无法解析时按翻译失败策略处理，不得把模型的额外解释发给用户。

### D3. 大模型审核模式

- **默认建议：**“高风险阻止，中风险进入人工队列，低风险自动通过”。
- 审核由中央服务端调用大模型完成；本地仍保留长度、频率、文件类型和黑白名单等确定性规则。
- 审核发生在翻译前，译文再进行一次轻量审核。
- 含图片的消息优先使用支持视觉输入的审核模型；若审核模型不支持图片，默认进入人工审核，不把未审核图片直接放行。
- 被阻止的内容默认不向任何目标会话发送；中央控制面板保留脱敏后的审核事件。
- 人工审核支持“通过、拒绝、编辑后通过”。
- 人工审核超时默认拒绝，不自动放行。

### D4. 图片卡片内容与回复

- **已确认：**所有消息使用统一卡片样式：
  - 来源平台图标与来源群/频道；
  - 发送者头像与显示名；
  - 发送时间；
  - 原文；
  - 译文；
  - 原消息附图；
  - 消息 ID 的短标识；
  - 审核或翻译状态（仅必要时显示）。
- **已确认：**译文或主要展示内容位于卡片主体，原文放在卡片下方的半透明区域；半透明区域必须保持足够对比度和可读性。
- **已确认：**QQ 和 Discord 目标侧最终都只发送渲染图片，不额外发送可复制的原始文本。
- 回复消息在卡片中展示“回复对象 + 被回复消息摘要 + 被回复消息缩略图（如有）”。
- 如果中央服务端保存了被回复消息在目标平台的消息 ID，发送图片卡片时同时使用目标平台的原生回复能力；找不到映射时仍发送带引用区域的图片卡片，不丢弃回复上下文。
- 长文本自动分页为多张卡片；每张图显示页码。
- 图片过多时生成多张卡片，不制作低清晰度的超长图。

### D5. 固定三程序与控制面板

- **已确认：**使用中央服务端、QQ 节点、Discord 节点三套固定程序，不提供角色互换。
- 中央服务端运行完整控制面板，保存蓝图、已验证会话、大模型设置、审核队列和全链路日志。
- 两个节点运行轻量面板，只管理本端平台凭据、中央服务端连接、运行状态和本地日志。
- 节点面板默认只监听 `127.0.0.1` 或内网地址；需要远程访问时通过 SSH 隧道、VPN 或单独配置的安全反向代理进入。
- 中央服务端不保存 QQ 登录凭据和 Discord Bot Token，只保存节点身份与非敏感的平台能力信息。

### D6. 默认技术栈

- **默认建议：**
  - 后端：TypeScript + Node.js 当前长期支持版本；
  - Web API：Fastify；
  - 前端：React + Vite；
  - 中央数据库：PostgreSQL；
  - 节点本地队列与缓存：SQLite；
  - 节点通信：WSS；
  - 图片渲染：服务端 HTML/CSS 模板 + 无头浏览器截图；
  - 部署：Docker Compose；
  - 反向代理与 TLS：Caddy 或 Nginx；
  - Discord：官方 Bot API 对应的成熟 Node.js SDK；
  - QQ：OneBot 11 WebSocket 适配层。

不得在实现中无版本锁定地直接使用 `latest` 镜像或浮动依赖。

### D7. 大模型 API 与提示词

- **默认建议：**基础设置首先支持 OpenAI 兼容 API，字段包括 API Base URL、API Key、翻译模型、审核模型、超时、重试、并发和连接测试。
- 翻译与审核默认可使用同一个 API，但允许分别选择模型。
- API Key 加密保存，只允许覆盖更新，不允许明文回显或通过节点下发。
- 高级模式允许编辑：
  - 翻译系统提示词；
  - 翻译任务提示词模板；
  - 审核系统提示词；
  - 审核规则与输出判定提示词。
- 提示词采用“用户可编辑内容 + 程序固定协议外壳”。固定外壳负责 JSON Schema、超时、字段验证和防提示词注入，不能在界面中删除。
- 所有提示词都有默认模板、版本、修改历史、恢复默认、试运行和发布功能。
- 编辑中的提示词为草稿，只有点击发布后才用于生产消息。

### D8. 蓝图路由

- **默认建议：**蓝图采用有向无环图，首期禁止形成环路，避免消息循环和无限调用。
- 首期节点类型：
  - 已验证的 QQ 群或 Discord 频道（输入/输出）；
  - 消息类型过滤；
  - 文本条件过滤；
  - 大模型审核；
  - 大模型翻译；
  - 图片卡片渲染；
  - 人工审核；
  - 分流与汇合；
  - 延迟/限速；
  - 丢弃与日志标记。
- 允许一个来源连接多个目标，也允许多个来源汇入一个目标。
- 蓝图支持草稿、校验、模拟测试、发布、版本历史和一键回滚。
- 发布前必须检查：至少一个来源和目标、节点已连接、会话已验证、无环路、无悬空必需节点、模型配置可用。

### D9. 聊天会话验证

- **默认建议：**只有通过验证码验证的 QQ 群或 Discord 频道才能出现在蓝图的正式会话节点中。
- 添加会话时，管理员选择平台节点及候选群/频道，或手动填写平台 ID。
- 中央服务端生成一次性随机验证码，通过对应平台节点向目标会话发送验证卡片。
- 管理员在中央面板回填验证码；验证成功后保存会话。
- 验证码默认 10 分钟过期、仅可使用一次、限制重发频率，连续错误需要暂时锁定。
- 重新验证用于权限变化、Bot 被移除后重新加入、节点更换或会话身份信息异常等场景。

---

## 4. 总体架构

```text
QQ 群
  │
  ▼
QQ 适配器（独立进程）
  │ OneBot WebSocket
  ▼
QQ 节点 ──出站 WSS──▶ 中央服务端 ◀──出站 WSS── Discord 节点
  │                   │   │   │                         │
  ├─ 本地可靠队列      │   │   ├─ PostgreSQL             ├─ 本地可靠队列
  ├─ 轻量控制面板      │   │   ├─ 大模型翻译与审核       ├─ 轻量控制面板
  └─ 媒体暂存          │   │   ├─ 蓝图路由与会话库       └─ 媒体暂存
                      │   │   ├─ 图片卡片渲染             │
                      │   │   └─ 完整 Web 控制面板        │ Discord Bot API
                      │   │                              ▼
                      │   └──────────────────────── Discord 频道
                      └─ 唯一 HTTPS/WSS 公网入口
```

中央服务端是唯一固定服务端，承担以下职责：

- 提供 HTTPS/WSS 入口并接收 QQ、Discord 节点主动连接；
- 完成节点注册、配对、身份验证、密钥轮换和吊销；
- 保存已验证聊天会话、蓝图草稿和已发布版本；
- 根据已发布蓝图编排消息处理；
- 调用大模型完成翻译和审核；
- 处理人工审核队列；
- 渲染最终图片卡片；
- 协调确认、重试、死信和全链路日志。

QQ 与 Discord 节点不承担中央职责，也不直接访问中央数据库。两个节点只需允许出站 HTTPS/WSS 流量，因此不需要路由器端口映射。

### 4.1 三套程序的职责边界

| 程序 | 保存的敏感信息 | 核心职责 | 不允许承担 |
|---|---|---|---|
| 中央服务端 | 大模型 API Key、节点身份密钥 | 蓝图、会话、翻译、审核、渲染、队列、日志 | QQ 登录、Discord Bot 登录 |
| QQ 节点 | QQ/OneBot 凭据、节点身份密钥 | QQ 收发、标准化、本地队列、媒体暂存 | 大模型配置、全局路由、Discord 登录 |
| Discord 节点 | Discord Bot Token、节点身份密钥 | Discord 收发、标准化、本地队列、媒体暂存 | 大模型配置、全局路由、QQ 登录 |

---

## 5. 消息处理流程

### 5.1 正常流程

1. QQ 或 Discord 平台适配器收到消息事件。
2. 来源节点过滤机器人自身消息和本项目生成的消息。
3. 来源节点将平台消息转换为统一消息格式，并写入本地可靠发送队列。
4. 来源节点通过 WSS 将事件上传中央服务端；媒体使用受限的分块上传或短期签名地址传输。
5. 中央服务端按平台消息 ID、节点 ID 和内容指纹进行幂等检查。
6. 中央服务端读取当前已发布的蓝图版本，查找与来源会话匹配的入口。
7. 判断消息类型：
   - 文字：提取纯文本；
   - 图片：安全下载到临时存储；
   - 文字加图片：同时保留；
   - 回复：解析被回复消息 ID、发送者、文字摘要和图片缩略图，并查询跨平台消息映射；
   - 其他：转换为“不支持的消息”占位内容。
8. 按蓝图依次执行过滤、本地规则、大模型审核、翻译、人工审核和限速节点。
9. 大模型审核和翻译结果必须通过固定 JSON Schema 校验。
10. 中央服务端渲染最终图片卡片。
11. 中央服务端为蓝图中的每个目标创建独立发送任务，并通过 WSS 下发到相应目标节点。
12. 目标节点校验签名、时间戳、序号、目标会话权限和消息大小。
13. 目标节点将卡片发送到目标群或频道。
14. 目标节点返回平台消息 ID 和确认；中央服务端记录来源与各目标消息的映射。
15. 失败时按策略重试，超过阈值进入死信队列；节点离线时中央服务端保留任务。

### 5.2 人工审核流程

1. 蓝图包含人工审核节点，或大模型判定为中风险时，消息进入待审核队列。
2. 中央控制面板显示原文、译文、图片缩略图、来源、目标、模型结论和命中原因。
3. 管理员选择通过、拒绝或编辑后通过。
4. 操作写入不可变更的审计记录。
5. 通过后继续渲染和转发；拒绝后结束任务。

### 5.3 不支持的消息

不支持类型不得静默丢弃。目标侧发送统一卡片，正文固定为：

```text
不支持的消息
Unsupported message type
```

卡片可显示原始类型名称，例如“语音”“文件”“红包”或“未知”，但不得尝试展示敏感的原始负载。

---

## 6. 统一消息模型

建议内部消息至少包含以下字段：

```json
{
  "schemaVersion": 1,
  "eventId": "uuid",
  "blueprintId": "uuid",
  "blueprintVersion": 1,
  "source": {
    "platform": "qq|discord",
    "spaceId": "群号或服务器ID",
    "channelId": "群号或频道ID",
    "messageId": "原平台消息ID"
  },
  "sender": {
    "id": "平台用户ID",
    "displayName": "显示名",
    "avatarUrl": "头像地址"
  },
  "sentAt": "ISO-8601",
  "kind": "text|image|mixed|unsupported",
  "text": "原文",
  "translatedText": "译文",
  "detectedLanguage": "zh|en|other",
  "attachments": [],
  "unsupportedType": null,
  "replyTo": {
    "sourceMessageId": "被回复消息在来源平台的ID",
    "targetMessageId": "可选：被回复消息在目标平台的ID",
    "senderDisplayName": "被回复者显示名",
    "textPreview": "被回复消息摘要",
    "imagePreview": "可选的受限媒体引用"
  },
  "moderation": {
    "status": "passed|pending|blocked",
    "riskLevel": "low|medium|high",
    "promptVersion": 1,
    "model": "string"
  },
  "translationPromptVersion": 1,
  "traceId": "uuid"
}
```

传输时不应长期依赖头像和图片的第三方 URL。媒体应在限定大小后上传中央服务端，或使用短期、带签名且受来源限制的下载地址。中央服务端处理完成后向目标节点下发最终卡片，不把大模型 API Key、提示词全文或其他节点凭据下发到平台节点。

---

## 7. 节点注册、配对与连接

### 7.1 推荐配对体验

1. 在公网服务器上部署中央服务端并完成首次管理员初始化。
2. 在中央控制面板选择“添加 QQ 节点”或“添加 Discord 节点”。
3. 中央服务端生成带节点类型限制的一次性配对码和固定 WSS 地址。
4. 在对应节点的轻量面板填写中央地址、节点名称和配对码。
5. 节点主动连接中央服务端并提交节点公钥、程序版本和平台类型。
6. 中央面板显示节点指纹、类型和名称，管理员确认。
7. 双方保存独立节点身份及加密后的长期令牌。
8. 一次性配对码立即失效；QQ 配对码不能注册 Discord 节点，反之亦然。

### 7.2 连接要求

- 生产环境只允许 `wss://`。
- 使用短期会话令牌，长期身份密钥支持轮换和吊销。
- 每条控制消息和数据消息都有唯一 ID、时间戳、序号及签名。
- 心跳间隔和断线重连采用带抖动的指数退避。
- 上传和下发消息都必须确认；未确认任务可重发，但接收方必须幂等。
- 中央服务端离线时来源节点把事件保存在本地队列。
- 目标节点离线时中央服务端把下发任务保存在 PostgreSQL。
- 设置单消息、单图片、单批次和队列总量限制。
- 系统时间偏差过大时拒绝敏感控制操作并提示校时。

### 7.3 公网 IP 的利用

中央服务端只开放：

- `443/tcp`：Web 控制面板、API 与 WSS；
- 可选 `80/tcp`：仅用于跳转到 HTTPS 或申请证书；
- SSH 管理端口应限制来源 IP 或使用 VPN。

QQ/Discord 服务端口、数据库端口、适配器端口不得直接暴露公网。

---

## 8. Web 控制面板

### 8.1 中央服务端控制面板

中央面板是日常管理的主要入口，至少包含：

1. **概览**
   - 中央服务端版本、运行时间和数据库状态；
   - QQ/Discord 节点在线状态、程序版本、最后心跳和平台登录状态；
   - 今日接收、转发、拦截、失败和待审核数量；
   - 中央队列、节点本地队列摘要、大模型耗时和最近错误。

2. **聊天会话**
   - 分页展示所有已保存的 QQ 群和 Discord 频道；
   - 按平台、节点、名称、平台 ID 和验证状态检索；
   - 添加、发送验证码、回填验证码、重新验证、禁用和删除；
   - 展示 Bot 权限检查、最后验证时间、最后成功发送时间和所在节点；
   - 未验证或已失效的会话不能发布到蓝图。

3. **消息蓝图**
   - 画布、节点库、缩放、小地图、框选、复制和撤销；
   - 从“聊天会话”拖入来源或目标节点；
   - 通过连线表达处理顺序和转发方向；
   - 节点侧边栏编辑过滤条件、语言方向、审核阈值、提示词版本、限速和失败策略；
   - 草稿自动保存；
   - 提供结构校验、模拟消息试跑、发布、停用、版本历史和回滚；
   - 正式执行只读取已发布快照，不直接读取正在编辑的画布。

4. **基础设置**
   - 大模型 API Base URL；
   - API Key 的设置/覆盖；
   - 翻译模型和审核模型；
  - 请求超时、重试次数、并发上限、最大输出长度和连接测试；
  - 审核模型是否支持视觉输入，以及图片审核不可用时的处置；
   - 默认源/目标语言和翻译失败策略；
   - 默认审核阈值和审核服务失败策略；
   - 卡片主题、字体、尺寸和数据保留周期；
   - 基础设置提供安全默认值，不要求普通用户理解提示词。

5. **高级模式**
   - 翻译系统提示词与任务模板；
   - 审核系统提示词、审核规则和分级判定模板；
   - 固定输入变量列表和预览；
   - JSON Schema 输出预览；
   - 草稿、差异比较、试运行、发布、恢复默认和版本回滚；
   - 显示提示词修改可能导致漏审、误审或翻译异常的警告；
   - 不允许删除程序固定的结构化输出和安全协议外壳。

6. **人工审核**
   - 待审核、已通过、已拒绝和超时队列；
   - 查看原文、译文、附件缩略图、来源、目标、模型结论和理由；
   - 通过、拒绝、编辑后通过和批量操作；
   - 所有操作记录操作者、时间和变更前后摘要。

7. **节点管理**
   - 生成限定类型的一次性节点配对码；
   - 查看节点身份指纹、版本、能力、平台状态和本地队列摘要；
   - 连接测试、撤销配对、密钥轮换和安全升级提示；
   - 只能下发白名单内的配置和诊断操作，不能执行任意命令。

8. **卡片样式**
   - 主题、字体、最大宽度；
   - 是否显示原文、译文、来源和时间；
   - 中英文、Emoji、长文本、多图和异常状态实时预览。

9. **日志与任务**
   - 按时间、平台、蓝图、会话、状态和追踪 ID 检索；
   - 查看单条消息的完整蓝图执行轨迹；
   - 导出脱敏日志；
   - 重试失败任务和管理死信队列；
   - 查看管理员审计日志和大模型调用用量。

10. **系统**
    - 管理员账号与会话；
    - 备份、恢复；
    - 数据保留周期；
    - 版本和健康检查；
    - 安全更新提示。

### 8.2 QQ 节点轻量面板

- NapCat OneBot 11 WebSocket 地址、鉴权信息、连接状态和测试；
- QQ 登录状态、当前账号、NapCat 版本和群列表发现状态；
- 中央服务端地址、节点配对、证书指纹和重连状态；
- 本地队列长度、媒体缓存、最近错误和脱敏日志；
- Token 只允许重新设置，不允许明文回显；
- 不提供蓝图、大模型 API、提示词或全局审核配置。

### 8.3 Discord 节点轻量面板

- Discord Bot Token 的设置、覆盖、连接测试和登录状态；
- Bot 身份、服务器/频道发现状态及权限诊断；
- 中央服务端地址、节点配对、证书指纹和重连状态；
- 本地队列长度、媒体缓存、最近错误和脱敏日志；
- Token 只允许重新设置，不允许明文回显；
- 不提供蓝图、大模型 API、提示词或全局审核配置。

### 8.4 聊天会话验证码流程

1. 节点定期或按需向中央服务端上报可见群/频道的非敏感摘要。
2. 管理员在“聊天会话”页面选择节点和目标会话，点击“发送验证码”。
3. 中央服务端生成高强度随机验证码及待验证记录，默认 10 分钟有效。
4. 中央服务端向对应节点下发验证任务。
5. 节点使用 Bot 在目标群/频道发送验证卡片，卡片包含验证码、过期时间和安全提示。
6. 管理员从目标群/频道读取验证码并回填到中央面板。
7. 中央服务端使用恒定时间比较校验验证码，成功后把会话标记为“已验证”。
8. 验证失败、过期、Bot 无发送权限或节点离线时展示明确原因；不得自动保存为可用会话。

删除已验证会话前，应检查它是否被已发布蓝图引用；如被引用，必须先停用或修改相关蓝图。

---

## 9. 翻译设计

翻译由中央服务端的大模型提供者层执行，并使用统一接口避免绑定单一供应商：

```ts
interface LlmTranslationProvider {
  translate(input: TranslationInput): Promise<TranslationResult>;
}
```

要求：

- 首期支持 OpenAI 兼容 API，也允许后续增加供应商原生接口和本地模型。
- 输入包含原文、期望目标语言及必要上下文，但默认不把无关历史聊天发送给模型。
- 输出至少包含检测语言、译文和置信度，并通过 JSON Schema 验证。
- 相同文本、源语言、目标语言、模型和提示词版本可缓存结果。
- 保留原文；不得只保存译文。
- 翻译超时默认仍可发送原文，并在卡片标注“翻译暂不可用”；也可按路由配置为进入重试队列。
- 文本发送到第三方翻译服务前应在面板明确提示数据会离开本机。
- 日志中不得完整记录供应商 API 密钥。
- 消息正文被视为不可信数据；提示词明确要求模型不得执行消息正文中的指令。
- 高级模式发布新提示词后，只影响新进入的消息；处理中的任务继续使用创建时记录的提示词版本。

---

## 10. 审核设计

审核由中央服务端的本地确定性规则和大模型审核层共同组成：

1. 本地规则：长度、频率、重复刷屏、黑白名单、关键词、URL 和附件类型。
2. 大模型审核：根据高级模式中已发布的审核提示词进行文本风险分类。
3. 图片审核：当所选模型明确支持视觉输入且管理员启用时，把经过尺寸和格式限制的图片副本交给模型；否则含图片消息默认进入人工审核。
4. 结果聚合：取最高风险级别，并记录本地规则、大模型、模型版本和提示词版本。

大模型审核结果必须符合固定结构：

```json
{
  "riskLevel": "low|medium|high",
  "decision": "allow|review|block",
  "categories": ["string"],
  "reason": "string",
  "confidence": 0.0
}
```

解析失败、字段越界、模型拒答或返回非预期内容时，按“审核服务故障”处理，不能默认放行。

默认处置：

| 风险 | 处置 |
|---|---|
| 低 | 自动通过 |
| 中 | 进入人工审核 |
| 高 | 自动阻止 |
| 审核服务故障 | 按路由配置；默认进入人工审核 |

管理员编辑后通过时，必须同时保留原文哈希、编辑后文本、操作者和时间，但普通日志界面应按数据保留策略脱敏。

---

## 11. 图片卡片渲染

渲染要求：

- 使用服务端固定模板生成 PNG，避免不同平台 Markdown 行为差异。
- QQ 和 Discord 两侧的桥接消息最终都只发送 PNG 卡片，不附带额外文本正文。
- 卡片主体显示译文或主要内容；原文固定显示在下方半透明面板中。
- 半透明面板的前景与背景对比度必须满足可读性要求；不能仅靠降低文字透明度实现。
- 回复消息在主体上方显示紧凑引用区，包括被回复者、文本摘要和可选图片缩略图。
- 若存在目标平台消息 ID 映射，目标节点还应将图片卡片作为原生回复发送；映射不存在时降级为普通图片消息，但保留卡片引用区。
- 默认支持中文、英文、Emoji；随部署包提供或安装明确授权的字体。
- 头像下载失败时使用平台默认头像。
- 对所有外部图片验证 MIME、实际文件头、尺寸和大小。
- 禁止渲染器访问本机环回地址、云元数据地址和内网敏感网段，防止 SSRF。
- 清理 HTML，禁止将消息内容作为可执行 HTML 注入。
- 超长单词、URL、代码块和 Emoji 必须正确换行。
- 卡片应设置像素与文件大小上限，超限时自动缩放或分页。
- 临时媒体在发送完成或超时后清理。

---

## 12. 日志、队列与数据保留

### 12.1 日志类型

- 应用运行日志；
- 消息处理日志；
- 中央与节点连接日志；
- 翻译和审核调用日志；
- 管理员操作审计日志；
- 安全事件日志。

### 12.2 日志字段

至少包含：

- 时间；
- 日志级别；
- 节点 ID；
- 蓝图 ID 与版本；
- 会话 ID；
- 大模型、提示词版本；
- `traceId`；
- 事件阶段；
- 耗时；
- 结果；
- 可安全展示的错误码。

不得记录：

- Bot Token；
- QQ 登录凭据；
- 配对长期密钥；
- Cookie；
- 完整 Authorization 头；
- 未脱敏的第三方 API 密钥。

### 12.3 默认保留策略

- 运行日志：30 天；
- 转发元数据：90 天；
- 临时媒体：成功后立即清理，失败任务最长保留 24 小时；
- 管理员审计日志：180 天；
- 被拒绝内容：仅保存必要摘要、哈希与命中规则，默认不长期保存原始敏感内容。

### 12.4 可靠性

- 任务状态：`received → blueprint_matched → moderating → translating → rendering → queued → sent → acknowledged`。
- 异常状态：`pending_review | retrying | blocked | dead_letter`。
- 重试采用指数退避并设最大次数。
- 每个平台消息 ID 只允许产生一次成功的目标发送。
- 服务重启后未完成任务可以恢复。

---

## 13. 安全要求

- Web 面板首次启动强制创建管理员。
- 管理员密码使用强密码哈希；支持后续增加 TOTP。
- 登录、配对、重试和测试消息接口均限速。
- 使用安全 Cookie、CSRF 防护、内容安全策略和严格跨域配置。
- 所有密钥从环境变量、Docker Secret 或受限配置文件读取。
- 配置导出默认排除密钥。
- 数据库与备份加密至少应依赖磁盘加密；敏感字段额外应用层加密。
- 节点控制指令采用明确白名单，不允许远程执行任意 Shell 命令。
- 上传及下载媒体设置大小、类型、超时和并发限制。
- 依赖锁定版本，定期进行漏洞检查。
- 管理面板建议放在 VPN、IP 白名单或额外身份认证之后。

---

## 14. 异常与边界行为

| 场景 | 预期行为 |
|---|---|
| 中央服务端离线 | 来源节点本地持久排队，恢复后继续上传 |
| 目标节点离线 | 中央服务端持久排队，节点恢复后继续下发 |
| 翻译超时 | 默认发送原文并标记翻译失败，可按路由改为重试 |
| 大模型审核服务离线或输出无效 | 默认转人工队列 |
| 头像下载失败 | 使用默认头像 |
| 附图下载失败 | 生成含“图片获取失败”的卡片 |
| 消息类型未知 | 生成“不支持的消息”卡片 |
| 重复事件 | 幂等过滤，不重复发送 |
| 机器人收到自己生成的卡片 | 过滤，禁止形成循环 |
| 目标平台限流 | 尊重重试时间，队列延迟发送 |
| 数据库暂时锁定 | 有限重试并报警，不丢任务 |
| 卡片过大 | 自动分页、压缩或拆分 |
| 配对密钥失效 | 停止对应节点传输并在中央与节点面板告警 |
| 中央服务端重启 | 两个节点自动退避重连 |
| 验证码错误或过期 | 不保存会话，限速后允许重新发送 |
| 验证会话无发送权限 | 返回权限诊断，不生成有效验证码 |
| 蓝图存在环路或未验证会话 | 禁止发布并定位错误节点 |
| 提示词输出不符合 Schema | 视为模型调用失败，不直接采用输出 |

---

## 15. 建议的仓库结构

```text
DisQord/
├─ apps/
│  ├─ central-server/       # 固定中央服务端
│  ├─ central-web/          # 完整中央控制面板
│  ├─ qq-node/              # 固定 QQ/NapCat 节点
│  ├─ discord-node/         # 固定 Discord 节点
│  └─ node-web/             # 节点共用的轻量控制面板组件
├─ packages/
│  ├─ shared/               # 类型、校验、错误码
│  ├─ transport/            # 节点到中央的 WSS、配对、确认、重试、签名
│  ├─ queue/                # 持久任务队列
│  ├─ adapter-napcat/       # NapCat/OneBot 11 适配器
│  ├─ adapter-discord/      # Discord 适配器
│  ├─ llm/                  # 大模型 API、翻译、审核与 Schema 校验
│  ├─ blueprint/            # 蓝图模型、校验、编译与版本
│  ├─ chat-session/         # 会话发现、验证码与验证状态
│  ├─ card-renderer/        # 图片卡片模板与渲染
│  └─ observability/        # 日志、指标、追踪
├─ deploy/
│  ├─ central/
│  ├─ qq-node/
│  ├─ discord-node/
│  └─ reverse-proxy/
├─ docs/
├─ tests/
├─ .env.example
├─ docker-compose.example.yml
└─ PROJECT_REQUIREMENTS.md
```

三套程序必须拥有固定入口和固定角色。QQ、Discord 节点可共享传输、队列、日志和轻量面板组件，但不得用运行时配置把同一个可执行程序切换成中央服务端。中央服务端与平台节点应分别构建镜像。

---

## 16. 分阶段实施计划

后续模型必须按阶段执行；每阶段完成后先报告验证结果，不应一次性跳到生产部署。

### 阶段 0：仓库基础

1. 初始化 Git。
2. 建立工作区、代码格式化、静态检查和测试框架。
3. 创建 `.env.example`、密钥忽略规则和基础文档。
4. 固定运行时及依赖版本。

完成标准：

- 安装、检查、测试和构建命令可执行；
- 仓库没有密钥；
- 空白应用可以启动。

### 阶段 1：统一模型与本地模拟链路

1. 定义消息、聊天会话、蓝图、任务、提示词版本、审核和日志数据结构。
2. 实现数据库迁移。
3. 实现中央服务端、模拟 QQ 节点和模拟 Discord 节点。
4. 实现幂等、状态机和失败重试。

完成标准：

- 模拟 QQ 与 Discord 节点都只能通过中央服务端交换消息；
- 重复事件不会重复发送；
- 重启后队列可恢复。

### 阶段 2：中央服务端与节点连接

1. 实现中央服务端 WSS 服务。
2. 实现 QQ、Discord 节点主动连接、心跳、退避重连。
3. 实现限定节点类型的一次性配对码、身份确认、签名和密钥轮换。
4. 实现任务确认与死信队列。

完成标准：

- 两个节点无需公网入站端口即可工作；
- 节点不能互相直连或切换成中央角色；
- 断网、重启、消息重放测试通过；
- 无效签名与过期消息被拒绝。

### 阶段 3：聊天会话与平台适配器

1. 接入 Discord 测试服务器和测试频道。
2. 通过 NapCat/OneBot 11 接入测试 QQ 群。
3. 实现文字、图片、混合消息和不支持类型。
4. 实现平台限流及自身消息过滤。
5. 实现候选会话发现、发送验证码、回填、过期、重发限速和重新验证。

完成标准：

- 两个平台测试空间内实现双向事件接收；
- 不产生循环；
- 不支持类型行为符合本文件；
- 未验证会话不能用于正式路由；
- 只有在目标群/频道实际读取验证码并正确回填后才保存为已验证。

### 阶段 4：大模型、提示词与卡片

1. 实现 OpenAI 兼容大模型提供者、加密 API Key 存储和连接测试。
2. 实现大模型翻译、审核、固定 Schema 校验及失败策略。
3. 实现翻译/审核提示词的默认模板、草稿、试运行、发布、历史和回滚。
4. 实现本地审核规则与人工审核状态机。
5. 实现统一卡片模板、分页与安全媒体下载。
6. 对中英、Emoji、长文本、坏图和模型异常输出进行测试。

完成标准：

- 中英双向翻译保留原文；
- 高风险阻止、中风险待审；
- 卡片在 QQ 和 Discord 中清晰可读；
- 编辑中的提示词不会影响生产，发布后新任务使用新版本；
- 模型返回非结构化或恶意输出时不会绕过审核。

### 阶段 5：中央控制面板与蓝图

1. 实现登录和权限基础。
2. 实现概览、聊天会话、基础设置、高级模式、人工审核、节点、卡片和日志页面。
3. 实现蓝图画布、节点库、属性编辑、校验、模拟试跑、发布、版本和回滚。
4. 实现 QQ 与 Discord 节点轻量面板。
5. 实现节点状态与安全的白名单配置下发。
6. 实现日志脱敏和管理员审计。

完成标准：

- 常用配置不需要手改数据库；
- 密钥不会明文回显；
- 可从中央面板判断整个链路在哪一步失败；
- 可以只通过拖放和连线完成 QQ 群与 Discord 频道的一对一、一对多和多对一规则；
- 非法蓝图不能发布，已发布蓝图不受草稿编辑影响。

### 阶段 6：容器化与部署文档

1. 制作中央服务端、QQ/NapCat 节点和 Discord 节点镜像及 Compose 示例。
2. 配置 TLS、健康检查、资源限制和日志轮转。
3. 编写备份、恢复、升级、回滚和故障排查步骤。
4. 在三台干净服务器上完成推荐部署演练。
5. 额外验证中央服务端与某一节点同机、但程序和数据隔离的节省成本部署。

完成标准：

- 按文档可从零部署；
- QQ、Discord 节点只需出站网络；
- 重启服务器后服务自动恢复；
- 备份可实际恢复。

---

## 17. 后续操作细节（供更换模型后继续）

用户批准本文档并明确说“继续”后，下一位执行模型应按以下顺序工作：

1. 先重新读取本文件，不要依赖聊天摘要猜测需求。
2. 检查目录、Git 状态和是否出现用户新增文件。
3. 如用户未修改决策项，采用第 3 节全部默认建议。
4. 只执行“阶段 0”，不要直接部署或登录真实 QQ/Discord 账号。
5. 创建实现计划并标明正在进行的唯一阶段。
6. 所有文件修改使用小步提交式组织，但未经用户要求不得推送远端或创建 PR。
7. 每完成一个子阶段，执行静态检查、单元测试和构建。
8. 报告：
   - 新增或修改了哪些文件；
   - 运行了哪些验证；
   - 哪些测试通过或失败；
   - 下一阶段需要用户提供什么。
9. 在需要真实凭据时，只创建 `.env.example` 占位符，绝不要求用户在聊天中粘贴 Token、Cookie 或密码。
10. 在选择 NapCat、Node.js、Discord SDK、PostgreSQL 和镜像版本前，查阅当时的官方文档或项目发布页，不使用可能过时的记忆。
11. 不得擅自：
    - 登录用户账号；
    - 开放公网端口；
    - 修改服务器防火墙；
    - 购买翻译/审核服务；
    - 推送 GitHub；
    - 删除用户文件；
    - 将真实消息发送到第三方测试服务。
12. 若发现本文档与用户后续明确指示冲突，以用户最新明确指示为准，并同步更新本文档。

### 阶段 0 建议命令目标

具体包管理器和版本在实施时确认，但仓库最终至少提供以下稳定入口：

```text
安装依赖
启动开发环境
执行静态检查
执行类型检查
执行单元测试
执行生产构建
启动中央服务端
启动 QQ 节点
启动 Discord 节点
执行数据库迁移
```

这些入口应封装为项目脚本，用户不需要记住内部工具命令。

---

## 18. 测试矩阵

### 消息类型

- 中文短文本；
- 英文短文本；
- 中英混合；
- Emoji；
- 超长文本；
- URL；
- 单图；
- 多图；
- 文字加图；
- 回复文字；
- 回复图片；
- 回复尚未转发或消息映射已丢失的内容；
- GIF；
- 语音、视频、文件等不支持类型；
- 删除或撤回事件（首期不转发，只记事件）；
- 机器人自身消息。

### 网络与可靠性

- 中央服务端先启动；
- 两个平台节点先启动；
- 中央服务端重启；
- QQ 节点重启；
- Discord 节点重启；
- 网络中断后恢复；
- 重复投递；
- 确认包丢失；
- 中央离线期间来源节点积压；
- 目标节点离线期间中央积压；
- 平台限流；
- 队列满；
- 磁盘空间不足告警。

### 蓝图与聊天会话

- 正确验证码、错误验证码和过期验证码；
- 验证码重发限速；
- Bot 没有目标会话发送权限；
- 节点离线时请求发送验证码；
- 删除被已发布蓝图引用的会话；
- 一对一、一对多和多对一；
- 悬空节点、无目标、无来源和有环蓝图；
- 草稿编辑不影响已发布版本；
- 模拟试跑、发布、回滚和停用。

### 大模型与提示词

- OpenAI 兼容 API 连接测试；
- 翻译与审核使用相同或不同模型；
- 超时、限流、服务错误和无效 API Key；
- 非 JSON、缺少字段、字段越界和模型拒答；
- 消息正文包含提示词注入指令；
- 提示词草稿、试运行、发布、恢复默认和版本回滚；
- 模型或提示词版本写入任务追踪。

### 安全

- 无效配对码；
- 使用 QQ 配对码注册 Discord 节点；
- 重放已签名消息；
- 过期令牌；
- 超大图片；
- 伪造 MIME；
- 恶意 HTML/脚本；
- 内网 URL 与云元数据 URL；
- 暴力登录；
- 日志密钥泄漏扫描。

---

## 19. 首期验收标准

满足以下条件才算首期完成：

1. 中央服务端、QQ 节点和 Discord 节点是三套职责固定、可独立构建和部署的程序。
2. QQ 节点通过 NapCat/OneBot 11 工作，Discord 节点通过 Discord Bot API 工作。
3. 只有中央服务器需要公网 IP，两个平台节点无需端口映射即可稳定连接。
4. 节点不能互相直连，也不能切换成中央服务端。
5. QQ 群和 Discord 频道必须通过发送并回填一次性验证码完成保存。
6. 未验证会话不能发布到蓝图。
7. 可以在图形界面通过拖放和连线配置一对一、一对多和多对一转发。
8. 蓝图支持校验、模拟、发布、版本历史和回滚，草稿不影响生产。
9. 文字、图片、文字加图片和回复消息均生成带头像和内容的图片卡片。
10. 两个平台最终都只发送渲染图片，原文在卡片下方的半透明区域中显示。
11. 回复消息展示引用摘要；存在跨平台消息映射时同时使用目标平台原生回复，不存在时安全降级。
12. 其他类型生成“不支持的消息”卡片。
13. 基础设置可以配置 OpenAI 兼容的大模型 API、翻译模型和审核模型。
14. 高级模式可以安全编辑、试运行、发布和回滚翻译与审核提示词。
15. 中文与英文由大模型自动互译，并同时显示原文与译文。
16. 大模型审核可以阻止高风险内容，并把中风险内容送到人工审核页面。
17. 大模型输出不符合 Schema 时不会绕过审核或把异常解释发送给目标平台。
18. 断线重连后不会丢失已入队任务，也不会重复发送已确认任务。
19. 中央服务端可查看两个节点、所有会话、蓝图、审核队列和全链路日志。
20. QQ 与 Discord 节点均可通过轻量面板查看和配置本端连接状态。
21. 日志可以使用 `traceId` 定位完整链路，且不泄漏平台或大模型密钥。
22. 服务重启后配置、队列、蓝图版本和消息映射仍然存在。
23. 备份和恢复经过实际测试。
24. 部署、升级、回滚和常见故障排查文档完整。

---

## 20. 审批记录

请用户审批时确认以下内容：

- [ ] 接受 NapCat 接入普通 QQ 可能存在的风控风险。
- [x] D1：QQ 端固定使用 NapCat/OneBot 11。
- [ ] 同意 D2：保留原文并显示中英译文。
- [ ] 同意 D3：高风险阻止、中风险人工审核、低风险放行。
- [x] D4：两侧所有转发统一为图片卡片，原文在下方半透明显示，并支持回复消息。
- [x] D5：使用固定中央服务端、QQ 节点、Discord 节点三套程序。
- [ ] 同意 D6：默认技术栈。
- [ ] 同意 D7：OpenAI 兼容 API，提示词采用可编辑内容与固定协议外壳。
- [ ] 同意 D8：蓝图首期采用有向无环图及列出的节点类型。
- [ ] 同意 D9：聊天会话通过目标群/频道验证码回填完成验证。
- [ ] 同意首期范围与明确排除项。
- [ ] 同意审批后先只执行阶段 0。

审批方式：

- 若全部同意，回复：`批准，继续阶段 0`。
- 若需修改，直接列出决策项编号或章节，例如：`D3 改为所有消息自动通过`。
